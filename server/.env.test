# Test Environment Configuration
NODE_ENV=test

# Database
DATABASE_URL="postgresql://test:test@localhost:5432/uimp_test"
TEST_DATABASE_URL="postgresql://test:test@localhost:5432/uimp_test"

# JWT
JWT_SECRET="test-jwt-secret-key-for-testing-only-min-32-chars"
JWT_EXPIRES_IN="1h"

# Server
PORT=3002
CORS_ORIGIN="http://localhost:3000"

# Redis (disabled for tests)
REDIS_URL=""

# Email (disabled for tests)
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# Storage (mocked for tests)
STORAGE_PROVIDER="local"
LOCAL_UPLOAD_DIR="./test-uploads"

# AWS S3 (disabled for tests)
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION="us-east-1"
AWS_S3_BUCKET=""

# Azure Blob Storage (disabled for tests)
AZURE_STORAGE_ACCOUNT_NAME=""
AZURE_STORAGE_ACCOUNT_KEY=""
AZURE_STORAGE_CONTAINER="test-uploads"
# Mailtrap Sandbox SMTP Configuration
# Get these credentials from https://mailtrap.io/inboxes

# SMTP Host (always sandbox.smtp.mailtrap.io for Mailtrap Sandbox)
SMTP_HOST=sandbox.smtp.mailtrap.io

# SMTP Port (always 2525 for Mailtrap Sandbox)
SMTP_PORT=2525

# Your Mailtrap username (found in your inbox settings)
SMTP_USER=your_mailtrap_username

# Your Mailtrap password (found in your inbox settings)
SMTP_PASS=your_mailtrap_password

# From email address (can be any email, it's just for display)
SMTP_FROM=noreply@yourapp.com

# From name (displayed as sender name)
SMTP_FROM_NAME=Your App Name

# Optional: Frontend URL for password reset links
FRONTEND_URL=http://localhost:3000